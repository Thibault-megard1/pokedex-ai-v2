# ============================================================================
# POKEDEX AI V2 - ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env.local and configure your settings
# .env.local is ignored by git and should contain your secrets
# ============================================================================

# ----------------------------------------------------------------------------
# LLM PROVIDER CONFIGURATION
# ----------------------------------------------------------------------------
# Choose your AI provider: ollama | mistral | openai
# - ollama: FREE, runs locally (requires Ollama installed)
# - mistral: Paid API (requires Mistral API key)
# - openai: Paid API (not yet implemented)
LLM_PROVIDER=ollama

# ----------------------------------------------------------------------------
# OLLAMA CONFIGURATION (Local, FREE)
# ----------------------------------------------------------------------------
# Install Ollama: https://ollama.ai
# Pull a model: ollama pull mistral
# Verify: curl http://localhost:11434/api/tags

# Ollama API endpoint (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Model to use (recommended: mistral, llama3, or mistral-nemo)
# List available models: ollama list
OLLAMA_MODEL=mistral

# ----------------------------------------------------------------------------
# MISTRAL AI CONFIGURATION (Paid API)
# ----------------------------------------------------------------------------
# Get your API key from: https://console.mistral.ai
# Pricing: ~€0.001-0.003 per 1K tokens

MISTRAL_API_KEY=your_mistral_api_key_here
MISTRAL_MODEL=mistral-small-latest

# ----------------------------------------------------------------------------
# OPENAI CONFIGURATION (Not yet implemented)
# ----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com

OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# ----------------------------------------------------------------------------
# DATABASE & SESSION
# ----------------------------------------------------------------------------
# These are used for authentication and data persistence
# Generate a secure random string for JWT_SECRET

JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# 1. DO NOT commit .env.local to git (it's in .gitignore)
# 2. For Ollama (free): Set LLM_PROVIDER=ollama and install Ollama
# 3. For Mistral (paid): Set LLM_PROVIDER=mistral and add your API key
# 4. The app works without AI features if provider is unavailable (graceful degradation)
# ============================================================================
